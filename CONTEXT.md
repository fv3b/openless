# OpenLess Fluid

OpenLess fork 上的「Ghostwriter 流式浮框」层：语音给 AI 下指令的指令台——把口语流水账整理成 AI 能一次听懂的指令，卡词时帮你说清，调优过的好说法随叫随到。本档只收 Fluid 层词汇；上游 openless 自有词汇不在内。

## Language

### 实体

**常用语（snippet）**:
用户存下来的表述实体：触发词/别名 → 表述文本，带贴位（inline 进正文 / footnote 附注）与启用开关。
_Avoid_: 条目、动作、注入、词条

**候选（candidate）**:
LLM 说话中现场生成的表述选项，三类：精准词、候选表述、命名；未入库，被挑中才生效。
_Avoid_: 建议、提示

### 流程

**指令化（instructional）**:
润色流的 prompt 语义——去口水、理结构、把口语表述转成准确说法，不改用户意图。
_Avoid_: 润色（单独说“润色”时仍指这个动作，但禁止理解为文风美化）

**待融队列（pending merge）**:
说话中被挑中/命中但尚未进入润色文本的材料的暂存处；由后续段润色或尾段补润消化。

**命中（hit）**:
触发词在转写流中被字符串规则匹配到，常用语即刻生效。

**推荐（recommendation）**:
LLM 按当前内容从已启用的常用语库里挑出相关的几条，缩小选择范围；与候选同区展示。

**机械模式（mechanical mode）**:
润色流关闭后的行为态：贴生转写原话，footnote 附注块照拼，inline 常用语退化为文末拼接，候选流不触发。
_Avoid_: 纯净模式（那是 FluidSession 整个不创建）

**纯净模式**:
总开关关掉后 FluidSession 不创建，完全退化为上游纯听写：无浮框、无三流。

### 浮框区域

**指令预览**:
浮框中部大字区，显示“若此刻停下将贴给 AI 的完整结果”；与最终拼装同源，所见即所贴。

**转写流**:
浮框底部小字滚动区，显示说话原话，作上下文参照。

**候选区**:
浮框顶部浮出区域，含现场候选与推荐常用语两行，点选或口头选。
_Avoid_: 动作 chips
