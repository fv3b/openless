# OpenLess Ghostwriter

OpenLess fork 上的「Ghostwriter 流式浮框」层：语音给 AI 下指令的指令台——把口语流水账整理成 AI 能一次听懂的指令，卡词时帮你说清，调优过的好说法随叫随到。本档只收 Ghostwriter 层词汇；上游 openless 自有词汇不在内。（内部代号原为 `fluid`，2026-09-11 起全局改名 `ghostwriter`；capsuleStyle 样式值 `"fluid"` 属样式系统用户数据标识，不在此列。）

## Language

### 实体

**常用语（snippet）**:
用户存下来的表述实体：触发词/别名 → 文本，带种类（表述/背景）、附带背景（引用或手写，多条）与启用开关；背景落点（开头/文末）是全局设置（Ghostwriter 设置页签），对所有背景同时应用。
_Avoid_: 条目、动作、注入、词条

**候选（candidate）**:
LLM 说话中现场生成的**名字**选项：叫法（认出已有事物的本名，配一句外行能懂的大白话解释）与命名（新造事物的名字，配理由）；与卡词无关，说话里的说不清之点和现造的词都校准；**纯展示提示**——不可点选、无口头「用候选N」，用户自己说出来由润色吸收。
_Avoid_: 建议、提示、精准词、候选表述、名字校准

**任务书（task brief）**:
Ghostwriter 各功能交给 LLM 的任务说明正文，可查看、编辑、恢复默认；数据注入与输出结构由系统固定，不算任务书。
_Avoid_: 提示词（UI 与文档层不用；仅代码内部沿用 prompt）

**聊天记录（chat transcript）**:
对话会话中用户段与 AI 回话按发生顺序混排的记录；结束润色的输入；行语法【我】/【助手】由代码固定。

### 流程

**指令化（instructional）**:
润色流的 prompt 语义——去口水、理结构、把口语表述转成准确说法，不改用户意图。
_Avoid_: 润色（单独说“润色”时仍指这个动作，但禁止理解为文风美化）

**待融队列（pending merge）**:
说话中被挑中/命中但尚未进入润色文本的材料的暂存处；由后续段润色或尾段补润消化。

**命中（hit）**:
触发词在转写流中被字符串规则匹配到，常用语即刻生效。

**推荐（recommendation）**:
LLM 按当前内容从已启用的常用语库里挑出相关的几条，纯展示为可读的触发词菜单；用户看中哪个**直接读触发词**，命中机制自然接住。不点选、无口头「用常用语N」。
_Avoid_: 建议、动作选择

**沉淀（sedimentation）**:
把值得复用的说法收成常用语的动作统称；路径：管理页手动批量提取——从最近 3 天的语音记录选段，LLM 出候选（说法＋建议触发词＋例句），先编辑后保存，不经确认不进库。

**实时助手（assist）**:
说话中按停顿/句毕触发的单次 LLM 调用，一次协同产出候选与推荐（原常用语提醒已退役）；对话会话中改为产出回话与推荐。
_Avoid_: 扫描、轮询

**对话（conversation mode）**:
代笔的可选模式：对话热键开启的会话，AI 按设置的回话时机与追问深度开口，结束后整份聊天记录润写成指令。默认关；普通代笔会话一切不变。
_Avoid_: 实时聊天、聊天模式、独立胶囊样式

**回话（reply）**:
对话会话中 AI 的一次开口：一句短文本或闭嘴，承担意图澄清与命名校准；不产生生效动作（不进待融、无撤销），纠错靠嘴。
_Avoid_: 提示、建议、回复卡片

### 浮框区域

**指令预览**:
浮框中部大字区，显示“若此刻停下将贴给 AI 的完整结果”；与最终拼装同源，所见即所贴。

**转写流**:
浮框底部小字滚动区，显示说话原话，作上下文参照。

**候选区**:
浮框顶部浮出区域，含现场候选与推荐常用语；纯展示提示——不点选、无口头命令，用户自己说出对应说法（读触发词/说本名）由命中与润色接住。
